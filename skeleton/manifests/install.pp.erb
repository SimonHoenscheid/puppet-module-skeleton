# == Class <%= metadata.name %>::install
#
# This class is called from <%= metadata.name %>
#
class <%= metadata.name %>::install (
  $variable  = $<%= metadata.name %>::variable, #overwrite this value passing parameters to the base class <%= metadata.name %> using hiera! 
){

}
