# == Class <%= metadata.name %>::firewall
# This class is meant to be called from <%= metadata.name %>
# It configures the firewall for the service if needed
class <%= metadata.name %>::firewall (
$variable_name = hiera('<%= metadata.name %>::firewall::variable_name'),
) {
  include <%= metadata.name %>::params
  }
