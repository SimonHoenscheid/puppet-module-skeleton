# == Class: <%= metadata.name %>
#
# Full description of class <%= metadata.name %> here.
#
# === Parameters
#
# [*sample_parameter*]
#   Explanation of what this parameter affects and what it defaults to.
#
# === Authors
#
# Simon Hoenscheid <simon@hoenscheid-itconsulting.com>
#
# === Copyright
#
# Copyright 2013 Simon Hoenscheid <simon@hoenscheid-itconsulting.com>
#
class <%= metadata.name %> (
) inherits <%= metadata.name %>::params {

  # validate parameters here

  class { '<%= metadata.name %>::install': } ->
  class { '<%= metadata.name %>::config': } ~>
  class { '<%= metadata.name %>::service': } ->
# class { '<%= metadata.name %>::firewall': } ~>
# class { '<%= metadata.name %>::externalsystems': } ~>
# class { '<%= metadata.name %>::monitoring': } ~>
# class { '<%= metadata.name %>::backup': } ~>
  Class['<%= metadata.name %>']
}
